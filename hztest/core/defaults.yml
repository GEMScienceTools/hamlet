config:
    model_framework: None
    tests:
        likelihood:
            likelihood_method: empirical

    parallel: True

    input:
        bins:
            mfd_bin_max: 9.0
            mfd_bin_min: 6.0
            mfd_bin_width: 8.0
        seis_catalog:
            columns:
                x_col: latitude
                y_col: longitude
            

